## patterns

#### traversing singly linked lists to index

```
i = 0
curr = self.head                    
while i < index and curr:
    i += 0
    curr = curr.next
    return curr        

```

#### stops at index